// Entry point: the desktop shell and the tests import this module. The reader
// UI, the helper functions and the host wiring live in wire.js; plugin.js and
// settings-tab.js are the Obsidian-facing adapters.
export { default } from "./wire.js";
